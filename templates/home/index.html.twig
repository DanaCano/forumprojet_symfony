{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <main class="container">
        <div class="row">
            {# col-md-8 , une taille de 8 pour un écran medium:  @madia min-width 768px une largeur minimum et en dessous de ça il va prendre tout la place de l'écran #}
            <div class="col-md-8">
                <h3 class="pb-4 mb-4 border-bottom">Informations récentes</h3>
                {# Ici je prendre le nom de dossier article et de dossier list et j'ai fait un include pour afficher les articles récentes sur ma page home #}
                {# Il faut les recuperer avec un paramettre dasn mon HomeController #}
                <div id="article-list">
                    {% include 'article/list.html.twig' with { articles } %}
                </div>
            </div>

            <div class="col-md-4">
                <div class="position-sticky" style="top: 2rem;">
                    {# Ici c'est l'inclusion de mon template widget/about.html.twig #}
                    {% include 'widget/about.html.twig' %}
                    {% include 'widget/categories.html.twig' %}
                </div>

            </div>
        </div>

    </main>
{% endblock %}
